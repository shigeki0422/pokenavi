---
import { SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
import Sidebar from './Sidebar.astro';
---

<Sidebar />
<header>
	<nav>
		<h2><a href="/">{SITE_TITLE}</a></h2>
		<div class="internal-links">
			<HeaderLink href="/ranking">使用率ランキング</HeaderLink>
			<HeaderLink href="/pokemon">ポケモン情報</HeaderLink>
			<HeaderLink href="/blog">考察記事</HeaderLink>
		</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}
	nav a {
		padding: 1em 0.5em;
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}
	@media (max-width: 720px) {
		nav {
			flex-wrap: wrap;
			padding: 0.3rem 0;
		}
		h2 {
			width: 100%;
			text-align: center;
			padding: 0.3rem 0;
		}
		.internal-links {
			width: 100%;
			display: flex;
			justify-content: center;
			flex-wrap: wrap;
		}
		nav a {
			padding: 0.5em 0.5em;
			font-size: 0.9rem;
		}
	}
	/* PCではサイドバーがナビを担うのでヘッダー自体を隠す */
	@media (min-width: 1024px) {
		header {
			display: none;
		}
	}
</style>
