---
import { SITE_TITLE } from '../consts';

const TYPES: [string, string][] = [
	['ノーマル', 'normal'], ['ほのお', 'fire'], ['みず', 'water'], ['でんき', 'electric'],
	['くさ', 'grass'], ['こおり', 'ice'], ['かくとう', 'fighting'], ['どく', 'poison'],
	['じめん', 'ground'], ['ひこう', 'flying'], ['エスパー', 'psychic'], ['むし', 'bug'],
	['いわ', 'rock'], ['ゴースト', 'ghost'], ['ドラゴン', 'dragon'], ['あく', 'dark'],
	['はがね', 'steel'], ['フェアリー', 'fairy'],
];
const NUM: Record<string, string> = {
	normal: '00', fighting: '01', flying: '02', poison: '03', ground: '04', rock: '05',
	bug: '06', ghost: '07', steel: '08', fire: '09', water: '10', grass: '11',
	electric: '12', psychic: '13', ice: '14', dragon: '15', dark: '16', fairy: '17',
};
const path = Astro.url.pathname;
const isActive = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
---

<div class="sidebar-overlay" id="sidebar-overlay"></div>
<aside class="sidebar" id="sidebar">
	<a class="sb-brand" href="/">
		<img class="sb-logo" src="/logo-mark.png" alt="ポケナビ ロゴ" width="36" height="36" />
		<span class="sb-brand-text">{SITE_TITLE}</span>
	</a>
	<nav class="sb-nav">
		<a href="/" class:list={['sb-link', { active: isActive('/') }]}>ホーム</a>
		<a href="/ranking" class:list={['sb-link', { active: isActive('/ranking') }]}>使用率ランキング</a>
		<a href="/pokemon" class:list={['sb-link', { active: isActive('/pokemon') }]}>ポケモン情報</a>
		<a href="/blog" class:list={['sb-link', { active: isActive('/blog') }]}>考察記事</a>
	</nav>
	<div class="sb-section-label">タイプ別考察</div>
	<nav class="sb-types">
		{TYPES.map(([jp, slug]) => (
			<a href={`/type/${slug}/`} class:list={['sb-type', { active: path === `/type/${slug}/` }]}>
				<img src={`/images/types/type-${NUM[slug]}-${slug}.png`} alt={jp} />
				<span>{jp}</span>
			</a>
		))}
	</nav>
	<div class="sb-foot">
		<a href="/about">運営者情報</a>
		<a href="/privacy">プライバシーポリシー</a>
	</div>
</aside>

<style>
	.sidebar-overlay {
		display: none;
		position: fixed;
		inset: 0;
		background: rgba(0,0,0,0.35);
		z-index: 49;
		backdrop-filter: blur(2px);
	}
	.sidebar-overlay.open {
		display: block;
	}
	.sidebar {
		display: flex;
		flex-direction: column;
		position: fixed;
		top: 0;
		left: 0;
		width: 260px;
		height: 100vh;
		overflow-y: auto;
		background: #fff;
		border-right: 1px solid #e2e8f0;
		padding: 1rem 0;
		z-index: 50;
		/* モバイル: デフォルトは左に隠す */
		transform: translateX(-100%);
		transition: transform 0.28s cubic-bezier(0.4, 0, 0.2, 1);
	}
	.sidebar.open {
		transform: translateX(0);
	}
	@media (min-width: 1024px) {
		.sidebar {
			transform: translateX(0);
			width: 220px;
		}
		.sidebar-overlay {
			display: none !important;
		}
	}
	.sb-brand {
		display: flex;
		align-items: center;
		gap: 0.55rem;
		text-decoration: none;
		padding: 0.5rem 1.0rem 0.9rem;
		border-bottom: 1px solid #f1f5f9;
		margin-bottom: 0.2rem;
	}
	.sb-logo {
		width: 36px;
		height: 36px;
		border-radius: 8px;
		flex-shrink: 0;
		box-shadow: 0 1px 4px rgba(0,0,0,0.08);
	}
	.sb-brand-text {
		font-weight: 800;
		font-size: 1.1rem;
		color: rgb(var(--gray-dark));
		letter-spacing: -0.01em;
	}
	.sb-nav {
		display: flex;
		flex-direction: column;
	}
	.sb-link {
		padding: 0.6rem 1.2rem;
		color: rgb(var(--gray-dark));
		text-decoration: none;
		font-size: 0.92rem;
		font-weight: 600;
		border-left: 3px solid transparent;
	}
	.sb-link:hover {
		background: #f8fafc;
	}
	.sb-link.active {
		border-left-color: var(--accent);
		color: var(--accent);
		background: #fff7ed;
	}
	.sb-section-label {
		font-size: 0.7rem;
		font-weight: 700;
		color: rgb(var(--gray));
		letter-spacing: 0.05em;
		padding: 1rem 1.2rem 0.4rem;
		border-top: 1px solid #f1f5f9;
		margin-top: 0.5rem;
	}
	.sb-types {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2px 0;
		padding: 0 0.6rem;
	}
	.sb-type {
		display: flex;
		align-items: center;
		gap: 5px;
		padding: 5px 6px;
		border-radius: 6px;
		text-decoration: none;
		color: rgb(var(--gray-dark));
		font-size: 0.78rem;
	}
	.sb-type:hover {
		background: #f8fafc;
	}
	.sb-type.active {
		background: #fff7ed;
		color: var(--accent);
		font-weight: 700;
	}
	.sb-type img {
		width: 20px;
		height: 20px;
	}
	.sb-foot {
		display: flex;
		flex-direction: column;
		gap: 4px;
		margin-top: auto;
		padding: 1rem 1.2rem;
		border-top: 1px solid #f1f5f9;
	}
	.sb-foot a {
		font-size: 0.75rem;
		color: rgb(var(--gray));
		text-decoration: none;
	}
	.sb-foot a:hover {
		color: var(--accent);
	}
</style>

<script>
  const btn     = document.getElementById('menu-btn');
  const sidebar = document.getElementById('sidebar');
  const overlay = document.getElementById('sidebar-overlay');

  function open() {
    sidebar?.classList.add('open');
    overlay?.classList.add('open');
    btn?.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }
  function close() {
    sidebar?.classList.remove('open');
    overlay?.classList.remove('open');
    btn?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  btn?.addEventListener('click', () =>
    sidebar?.classList.contains('open') ? close() : open()
  );
  overlay?.addEventListener('click', close);

  // リンクをタップしたら閉じる（モバイルのみ）
  sidebar?.querySelectorAll('a').forEach(a =>
    a.addEventListener('click', () => {
      if (window.innerWidth < 1024) close();
    })
  );
</script>
