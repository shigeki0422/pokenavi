---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import GlobalSearch from '../../components/GlobalSearch.astro';
import rankingData from '../../data/ranking.json';
import { POKEMON_NAME_EN as NAME_EN } from '../../i18n/pokemon-names';

const { dates, pokemon } = rankingData as {
  dates: string[];
  pokemon: { name: string; id: string | null; dates: Record<string, { rank: number; rate: number | null }> }[];
};

const lastDate = dates[dates.length - 1];
const top10 = [...pokemon]
  .filter(p => p.dates[lastDate]?.rank)
  .sort((a, b) => (a.dates[lastDate]?.rank ?? 999) - (b.dates[lastDate]?.rank ?? 999))
  .slice(0, 10);

const fmtDate = (d: string) => { const [, m, day] = d.split('-'); return `${parseInt(m)}/${parseInt(day)}`; };


const siteUrl = 'https://pokenavi.jp';
const alternates = [
  { lang: 'ja', url: siteUrl + '/' },
  { lang: 'en', url: siteUrl + '/en/' },
  { lang: 'ko', url: siteUrl + '/ko/' },
  { lang: 'x-default', url: siteUrl + '/' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="Pokémon Champions Data Analysis | PokeNavi"
      description="Usage rate ranking, battle simulator, and analysis for Pokémon Champions. Data-driven competitive strategy."
      imageUrl="/og-image.png"
      alternates={alternates}
      ogLocale="en_US"
    />
    <style>
      .hero { text-align: center; padding: 3rem 1rem 2.5rem; border-bottom: 1px solid rgb(var(--gray-light)); margin-bottom: 2.5rem; }
      .hero h1 { font-size: 2.5rem; margin: 0 0 0.75rem; line-height: 1.2; }
      .hero p { font-size: 1.1rem; color: rgb(var(--gray)); margin: 0; }
      .section-title { font-size: 1.25rem; font-weight: 700; margin: 0 0 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid rgb(var(--accent)); color: rgb(var(--black)); }
      .bs-feature { border: 1px solid rgb(var(--gray-light)); border-radius: 14px; padding: 1.6rem 1.8rem; background: linear-gradient(135deg, #f5f7ff, #ffffff); box-shadow: 0 1px 4px rgba(0,0,0,0.05); }
      .bs-feature h3 { margin: 0 0 0.5rem; font-size: 1.2rem; color: rgb(var(--black)); }
      .bs-feature p { margin: 0 0 1.1rem; color: rgb(var(--gray)); font-size: 0.95rem; line-height: 1.7; }
      .bs-points { display: flex; flex-wrap: wrap; gap: 0.5rem; margin: 0 0 1.2rem; padding: 0; list-style: none; }
      .bs-points li { background: #eef2ff; color: var(--accent-dark); font-size: 0.8rem; font-weight: 700; padding: 0.25rem 0.7rem; border-radius: 9999px; }
      .bs-cta-row { display: flex; gap: 0.7rem; flex-wrap: wrap; }
      .bs-cta { display: inline-block; padding: 0.6rem 1.3rem; border-radius: 8px; text-decoration: none; font-weight: 700; font-size: 0.9rem; }
      .bs-cta.primary { background: var(--accent); color: #fff; }
      .bs-cta.primary:hover { background: var(--accent-dark); }
      .bs-cta.ghost { background: #fff; border: 1px solid var(--accent); color: var(--accent); }
      .bs-cta.ghost:hover { background: #eef2ff; }
      @media (max-width: 720px) { .hero h1 { font-size: 1.75rem; } }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <section class="hero">
        <h1>Pokémon Champions<br />Data Analysis</h1>
        <p>Usage rankings, battle simulation, and data-driven strategy for Pokémon Champions</p>
        <GlobalSearch variant="hero" lang="en" />
      </section>

      <section style="margin-bottom:3rem">
        <h2 class="section-title">Battle Simulator <span style="font-size:0.8rem;font-weight:400;color:rgb(var(--accent));margin-left:6px">Featured</span></h2>
        <div class="bs-feature">
          <h3>Diagnose your party's strength with AI round-robin battles</h3>
          <p>Test your party against all top-ranked trainers using a self-trained battle AI with look-ahead search. Get win rates, lead picks, 1v1 matchups, and replays — backed by data, not guesswork.</p>
          <ul class="bs-points">
            <li>All-ranker round-robin</li>
            <li>Win/loss path analysis</li>
            <li>1v1 matchup table</li>
            <li>Battle replays</li>
          </ul>
          <div class="bs-cta-row">
            <a class="bs-cta primary" href="/simulator">Run Top-Env Simulation →</a>
            <a class="bs-cta ghost" href="/battle-simulator">What is the Battle Simulator? →</a>
          </div>
        </div>
      </section>

      <section style="margin-bottom:3rem">
        <h2 class="section-title">Usage Ranking <span style="font-size:0.8rem;font-weight:400;color:rgb(var(--gray));margin-left:6px">TOP10 — Updated {fmtDate(lastDate)}</span></h2>
        <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(100px,1fr));gap:10px">
          {top10.map((p, i) => {
            const rank = p.dates[lastDate]?.rank ?? i + 1;
            const name = NAME_EN[p.name] ?? p.name;
            return (
              <a href="/en/ranking/" style="display:block;text-decoration:none;border:1px solid #e2e8f0;border-radius:8px;padding:10px 6px;text-align:center;transition:box-shadow 0.2s" onmouseover="this.style.boxShadow='0 4px 12px rgba(0,0,0,0.1)'" onmouseout="this.style.boxShadow=''">
                {p.id && <img src={`/images/pokemon/pokemon-${p.id}.webp`} alt={name} style="width:64px;height:64px;display:block;margin:0 auto 4px" loading={i < 4 ? 'eager' : 'lazy'} width="64" height="64" />}
                <div style="font-size:0.72rem;font-weight:bold;color:rgb(var(--gray));margin-bottom:1px">#{rank}</div>
                <div style="font-size:0.75rem;font-weight:bold;color:rgb(var(--black))">{name}</div>
              </a>
            );
          })}
        </div>
        <div style="text-align:center;margin-top:12px">
          <a href="/en/ranking/" style="display:inline-block;padding:10px 28px;background:var(--accent);color:white;border-radius:6px;text-decoration:none;font-size:0.9rem;font-weight:700;transition:opacity 0.2s" onmouseover="this.style.opacity='0.85'" onmouseout="this.style.opacity='1'">
            View full ranking (TOP200) →
          </a>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>
