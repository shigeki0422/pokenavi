[workspace]
resolver = "2"
members = ["engine", "wasm"]
exclude = ["pyengine"]  # R5でmembersへ移す

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[profile.release-wasm]
inherits = "release"
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
