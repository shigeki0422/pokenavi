# 保留中のAI挙動ケース（d2探索の限界に起因・将来の学習/深化で解消したいもの）

このファイルは「対戦シミュレータのAIが取った違和感のある手で、**現状は深さ2（d2）探索の限界として一旦保留している**ケース」の記録。
今後 **探索深化（d3）／相手モデル改善／価値ネット再学習** を行う際の **合否判定基準** として使う。
（常時読み込むCLAUDE.md/MEMORY.mdには載せない＝トークン節約。探索を触るとき search_ai.py から本ファイルを参照する。）

最終更新: 2026-06-17 / 対象AI_VER: `k4-megaeval-1mega-d2`（tree d2, tree_k=4, det≈16, maximin）
※**これらのケースは解消後も削除せず保持する**＝AI改善のたびに「担保（再発していないか）」を確認する回帰チェックとして使う。
再判定スクリプト: `scripts/_verify_deferred_cases.py`（cd scripts && ./venv/bin/python _verify_deferred_cases.py）。新AIを `AIS` に足して再実行する。

---

## ✅ 検証記録：MCTS(同時手番DUCT/regret)で解消（2026-06-17）
`search_ai.py` に IS-MCTS+Decoupled PUCT を実装（`mcts=True, mcts_select='regret'`）。`_verify_deferred_cases.py` で d2 と比較した結果、**根本原因（maximin/top-kが低確率の相手交代・無効phaze技を過大評価）が解消**：
- **ケース1（ガブHP112/C-1 vs ドリュウズ・ルカリオ自殺交代）**：d2は交代に均衡値0.369（じしん0.421と拮抗）で8回中1回交代。**MCTSは交代~0%**（じしん80%）。
- **ケース2（ガブ vs エルフーン・ドラゴンテール=フェアリーに0倍無効）**：d2は無効技に0.231（じしん0.321と僅差・det揺れで実戦反転）。**MCTSはドラゴンテール1%**（じしん99%）。
- **文脈識別も正しい**：ドラゴンテールの評価が vs エルフーン(無効・phaze不発)=1% / vs ドリュウズ(ドラゴン通る・つるぎのまい前に流せる)=20% と相手で適切に変わる。maximinの「一律過大評価」が消え、phaseが実際に効くかを見分けている。
- 注：勝率では d2 比 +数pt（regret@3200で53.5%, p0.048）と小さいが、**読み筋の質はこの通り明確に改善**（該当局面が稀で全体勝率に出にくいだけ）。
- **ケース3（ステロ下の自殺交代）**：局面再構成が複雑で個別再現は未実施。根本原因がケース1/2と同根なので同機構で解消の見込み。要・後日再現。

---

## 共通の根本原因（仮説）
いずれも **価値ネットの葉評価（材料・タイプ相性）は概ね正しい**が、以下の探索側の弱さで「読み筋」として悪手が選ばれる：

1. **相手モデル（maximin・top-k）が低確率の相手交代を過大評価**する。とくに「相手が引く → こちらの技で交代先を縛る/釣る」分岐の価値を高く見積もる。
2. **d2の地平線が浅く、相手の"最も自然な一手（主力STAB等）"で咎められる損を十分に減点できない**。
3. det（決定化）ノイズで僅差の手が時々逆転する。

→ 期待：**d3化／相手モデルで本命応手を厚くする／価値ネット再学習** のいずれかで、下記が「人間的に自然な手」に収束するはず。

## 再判定のやり方（共通）
各ケースの party спеック（下記）で局面を組み、`train_az2._net_ai(..., tree=True, tree_depth=2, tree_k=4, tree_det=16)` を作って
`ai.score_actions_tree(s1,s2,field)` の均衡値、および `ai(s1,s2,field)` の実選択を確認する。
深化後は **tree_depth=3** 等に上げて同じ局面で再評価し、「期待する手」が選ばれるかを見る。
（具体的な再現スクリプトの雛形はこのファイル末尾。）

---

## ケース1：過剰/甘い交代（ガブリアス → ルカリオ）
- **対戦**: M-1#1 サックー（自分） vs M-1#6 キョウタ（相手）
- **状況**: ガブリアス（自分・場）が、相手ドリュウズに対して **ルカリオへ交代**。ルカリオは交代先で **じしん（地面×2＝ルカリオのはがね/かくとうに抜群）を受けて落ちる**。
- **違和感**: ガブが既に場にいてドリュウズを処理できる状況で、ルカリオを差し出して落とすのは損に見える。
- **調査結果（事実）**:
  - 価値ネットの葉評価は正しい（残留してドリュウズ撃破＝高評価、ルカリオ交代死＝低評価）。
  - ただし **ルカリオはドリュウズに本来強い**（アイアンヘッドを0.25倍で半減、いわ等倍、インファイトはドリュウズに2倍）。**唯一じしん（ドリュウズの主力STAB）だけがルカリオに2倍**で刺さる。
  - d2のmaximinでは「相手の応手」が手ごとにズレて評価され、交代の最悪応手値が残留と拮抗～優位に出る。結果、交代が選ばれ得る。
- **保留理由**: ドミナンス除去（②）では消えない（探索の自己評価では劣位でない）。**「じしんという本命かつ唯一の刺し手に賭けない」読みの甘さ**＝相手モデル/深さの問題。
- **期待挙動（解消基準）**: ガブは**残留してじしん等で動く**（or じしんが来ない確証がある時のみ交代）。少なくとも「主力STABで一方的に咎められる交代」を既定で選ばない。
- **再現スペック**:
  - 自分(P1) ガブリアス@オボンのみ:いじっぱり:ドラゴンテール|じしん|ステルスロック|まきびし / フラエッテ(永遠)@フラエッテナイト / ルカリオ@ルカリオナイト:ようき:インファイト|コメットパンチ|ストーンエッジ|しんそく
  - 相手(P2) ドリュウズ（やわらかいすな・いじっぱり・じしん/アイアンヘッド/いわなだれ/つるぎのまい） / ルカリオ@ルカリオナイト / ゲッコウガ@こだわりスカーフ:へんげんじざい:なみのり/れいとうビーム/ヘドロウェーブ/あくのはどう
  - 局面：ガブ HP≈112/215・C-1、相手ドリュウズが場。

## ケース2：無効技を撃つ（ガブリアスがフェアリーにドラゴンテール）
- **対戦**: M-1#1 サックー（自分） vs M-1#2 攻め意識（相手）・3戦目・ターン3付近
- **状況**: ガブリアス（自分）が **エルフーン（くさ/フェアリー）にドラゴンテール（ドラゴン技）**。ドラゴンはフェアリーに **無効（0倍）**で、何も起きない。
- **違和感**: 0ダメの無効技を、しかも「おいかぜを展開して有利な（交代しそうにない）エルフーン」相手に撃つのは不自然。
- **調査結果（事実）**:
  - AIの被ダメ見積もりは正しい（ドラゴンテール→エルフーン=0.0、じしん=59を認識）。**ダメージ/無効のバグではない**。
  - 局面再構成では均衡値が **ドラゴンテール≈0.50 / じしん≈0.53 / ステロ≈0.58** と僅差。0ダメなのに高いのは、maximinが **「エルフーンが交代する分岐」でドラゴンテールが交代先を縛る価値**を計上しているため。det揺れで時々じしんを逆転する。
  - ※ドラゴンテールは無効相手には phaze（強制交代）も発生しない＝対面のエルフーンには完全に無価値。価値は「相手が引く」前提でのみ。
- **保留理由**: 相手モデルが低確率の交代を過大評価＋d2では「無駄ターン」を十分減点できない。ケース1と同根。
- **期待挙動（解消基準）**: 対面が無効相手のとき、**交代読みの phaze/ピボット技を主軸に選ばない**。じしん/ステルスロック等の有効手を選ぶ。
- **再現スペック**:
  - 自分(P1) ガブリアス@オボンのみ:いじっぱり:ドラゴンテール|じしん|ステルスロック|まきびし
  - 相手(P2) エルフーン@きあいのタスキ:おくびょう:ムーンフォース|おいかぜ|やどりぎのタネ|うそなき（いたずらごころ）
  - 局面：ガブ・エルフーンが対面、エルフーンはおいかぜ展開後。

## ケース3：ステルスロック下での瀕死級ポケモンの交代（交代しても再登場で落ちる）
- **対戦**: M-1#5 らいざ（自分） vs M-1#6 キョウタ（相手）・1戦目。
- **状況**: 自分側にステルスロック設置済み（T6 相手カバルドンが設置）。**リザードン（ほのお/ひこう）はSRで約50%(83ダメ)**を受ける。強制交代でリザが出て **16/167** まで減少（T7-8）。T9で自分は**リザ(16/167)を自発的に引っ込め**てミロカロスを出す（ミロもSRで25被弾）。リザは以後SR(83)>残HP(16)で**二度と安全に出せず**、T12に他に出せる駒が無く強制登場→SRで即瀕死(0)。
- **違和感**: SRが撒かれている状況で、SRに弱い瀕死級ポケモンを温存目的で引っ込めても、再登場すればSRで落ちるため温存にならない。「ほぼありえない」交代。出すこと自体が無価値化する。
- **原因（仮説）**: 探索/価値ネットが**交代時の設置ダメージ（特に再登場の不可逆な損失）を十分に織り込めていない**。d2では「引っ込めて安全」と短絡しがち。ケース1/2と同根の探索側の弱さ。
- **期待挙動（解消基準）**: 自分側に設置がある時、**設置で落ちる/大ダメージを受ける駒の出し入れを原則避ける**。瀕死級でSRに弱い駒は、温存より「場で仕事をさせる」を優先。再登場で確定で落ちる交代を既定で選ばない。
- **再現スペック（概要）**: 自分=ガブリアス/リザードン/ミロカロス、相手=ゲッコウガ/カバルドン/ギャラドス。相手カバルドンのステルスロック設置後、リザードンがSRで50%級を受ける局面。

---

## エンジンの既知改善点（AI挙動ではなくエンジン実装側。将来直すときの覚書）
- **表示ロジックの抜本対策（推奨・将来の全再計算時に）**：盤面の技表示(`parseActions`)と簡易ログ(`summarizeTurn`)は、現状**自由文ログを正規表現で逆解析**して技・側・ダメージを復元している。確認行の無い再使用技・守り系（「身を守っている」だけで技名が出ない）・無効/防がれた等で取りこぼしが起きやすく、都度パッチしている。**本来は `feature1.py` のターンスナップショットに各陣営の選択行動（技名 or 交代）を構造化フィールドとして記録**し、表示はそれを正典とすべき（ダメージ/付随効果のみログ補完）。データ作り直し（M-3対応の全再計算）時に導入するのが望ましい。

## 実験記録：探索深化(d3)は現行ネットで勝てない（2026-06-15）
- **メガ畳み込み**（`search_ai.py` `_candidate_actions` に `collapse_mega` トグル実装＝石持ちは常にメガ前提・メガ無し重複を列挙しない＝分岐半減）を入れ、**同一学習済みネット**で d3 vs 現状d2(k4) を直接対戦（`scripts/_mega_match.py`、選出は深さ非依存で中立、側交互）。
- **d3@k2(畳み込み)**：計749決着で **51.7%**（CI[48.1, 55.3], p≈0.36）＝**有意差なし**。
- **d3@k3(畳み込み)**：177決着で **47.5%**（p≈0.50）＝有意差なし（むしろ微負）。約164秒/戦で高コスト。
- **結論**：探索を深く/広くしても勝率が上がらない＝**ボトルネックは探索深さでなくネット（価値/方策）の質**。「探索を足しても伸びない＝ネットが頭打ち」の典型。
- **次の本命**：(a)**相手モデル改善**（maximinの相手手集合を policy 加重にし、低確率の相手交代の過大評価を是正＝保留ケース1/2に直接効く・安い）、(b)**価値/方策ネット再学習**（自己対戦反復増・~1〜1.5h/フル学習）。
- **メガ畳み込み**は単体で強くはならないが**分岐半減で低コスト化**に有効（採用しても損はない）。`collapse_mega` フラグは温存。
- d2深化で「じしん vs 交代」の差が広がる現象（前回の `_d3_probe.py`）は本物だが、**そういう局面が稀で全体勝率には出ない**ことが今回判明。
- **【2026-06-14 修正済】おやこあい（メガガルーラ）のダメージ処理／ログ順序**：以前は本体ヒットの判定・ログより**先に「おやこあい 追撃！」が出て**いた（追撃ブロックが本体サマリ行より前にあった）。
  - **修正内容**：`battle.py` `_execute_move` の追撃ブロックを **本体の `_apply_secondary`/`_apply_recoil` の後ろ（`return logs` 直前）へ移動**。これで「本体ヒット→本体サマリ行→本体副次→追撃→追撃副次」の正しい順で処理・記録される。本体サマリ行は本体ダメのみ（追撃は別行 `おやこあい 追撃！ N`）。`total_dmg += pb_dmg` は撤去（追撃は末尾処理で `_apply_recoil(pb_dmg)` を個別適用）。
  - **影響**：HP最終結果は不変。副次効果の発動順・反動の丸め・ドレイン/かいがらのすずが本体基準（ガルーラ現実型では無関係）。921テスト全PASS。
  - **データ反映**：n=1（公開済み）は旧エンジン。フェーズ2(n=2)以降の新規計算は修正後（ユーザー指示=単純増分なので、ガルーラ戦は1戦目旧・2戦目新の混在＝影響小）。
  - **表示側**：`simulator.astro` の `reorderLogs()`＋簡易ログ合算は旧フォーマット用に残置（新フォーマットでは追撃行が末尾＝swap発火せず無害）。新旧どちらのデータも正しく描画する。

## 参考：調査済みで「正しい」と確定したもの（保留ではない＝再学習で変わってはいけない）
- **ハラバリー が メガギャラドス に1v1で負ける**（M-1#2 vs M-1#5）: 正しい。ハラバリーは耐久型で電気技が低威力（ボルトチェンジ70/パラボラ65）＝2倍でも42%止まり・確3。メガギャラ（みず/あく）は電気2倍だが高耐久・高速(S127>58)・**じしんが電気に2倍(77%)**で上から2発。評価は妥当。

## 参考：既に解消した違和感（このAI_VERで修正済み）
- フラエッテが場に残るのにメガしない → **メガ・タイブレーク**で解消。
- スターミー/キラフロルの**2メガ選出** → 選出ロジックを**メガ後評価＋2メガ強制回避**に修正して解消。
- 1v1相性表・選出がメガを無視 → メガ後で評価するよう修正。

---

## 再現スクリプト雛形（参考）
```python
import random
from simulator.simulate import get_loader
from simulator.pokemon import build_from_spec, parse_pokemon_spec
from simulator.battle import BattleSide, BattleField
from simulator.belief import OpponentBelief
from train_az2 import _net_ai
from simulator.az_np import PVNetNP
L=get_loader(); net=PVNetNP.load()
def b(s): return build_from_spec(parse_pokemon_spec(s),L,season='M-2',randomize=False)
# … 上記スペックで s1,s2,field を組む。belief を付与 …
ai=_net_ai(net,L,0,12,0,tree=True,tree_depth=2,tree_k=4,tree_det=16)   # 深化時は tree_depth=3
sc=ai.score_actions_tree(s1,s2,field)   # 均衡値
print(sorted(sc,key=lambda x:-x[1]))
# ai(s1,s2,field) で実選択。複数seedで分布も見る。
```
