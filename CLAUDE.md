# ポケナビ プロジェクトルール

## 概要
ポケモン対戦データ分析サイト「ポケナビ」（https://pokenavi.jp）
VGC・ランクマッチ対戦勢 + DS学習者向け。月10万円不労所得が目標。

## 技術スタック
- **フレームワーク**: Astro v6（MDX、sitemap統合）
- **ホスティング**: Cloudflare Pages（GitHub Actions経由でデプロイ）
- **フォント**: Atkinson（ローカル配置）
- **サイトURL**: https://pokenavi.jp

## ディレクトリ構成
```
src/
├── assets/          # 画像・フォント
├── components/      # Astroコンポーネント
├── content/blog/    # ブログ記事（Markdown/MDX）
├── layouts/         # ページレイアウト
├── pages/           # ルーティング
└── styles/          # グローバルCSS
```

## 詳細規約の所在（作業前に必ず該当ファイルを読む）
常時ロードを避けるため、手続き・詳細規約は以下に分割している。**この役割分担は今後も維持する**：

- **CLAUDE.md** ＝ 常時必要な最小限のみ（概要・公開/デプロイの禁止則・データ取得の基本）
- **`.claude/skills/`** ＝ 特定作業の手続き・詳細ガイドライン（該当作業時のみ読む）
- **`.claude/rules/`** ＝ 特定パス配下の開発ルール（パススコープ）
- **`.claude/hooks/`** ＝ 機械的に強制すべき禁止・確認事項

新しい規約・手順を追加するときは上記に従って置き場所を決め、**CLAUDE.md に手続きを書き足して再肥大化させない**。

| 作業 | 正本 |
|---|---|
| 考察記事の執筆・加筆・推敲 | `.claude/skills/article-writing/SKILL.md`（So What原則・公平性・事実確認・型カード・画像番号表・SEO） |
| ポケモン情報ページの作成・更新 | `.claude/skills/pokemon-page/SKILL.md`（DBクエリ雛形・画像/スプライト・FAQ JSON-LD） |
| シミュレータ開発（scripts/simulator・scripts/tests） | `.claude/rules/simulator.md`（3点セット・監査スクリプト・盲点ゲート） |
| 公開前の記事レビュー | `.claude/commands/記事レビュー.md`（観点1〜20） |

## OGP画像ルール（必須）
- **`src/pages/` に新規ページを作成する際は、必ず `BaseHead` に `imageUrl` または `image` を指定する**
  - ブログ記事レイアウト（BlogPost.astro）: `image={heroImage}` → heroImageが自動でOGPになる
  - 固定ページ（simulator, tools等）: `imageUrl="/og-image.png"` → サイト共通OGPを使う
- 指定しないと Astro のデフォルト画像（"Build the web you want"）がXカードに表示される
- X投稿時はURLに `?v=X`（バージョン番号）を付けてキャッシュを回避する

## コンテンツ規約
- ブログ記事は `src/content/blog/` に Markdown で追加
- ファイル名: `{pokemon-name}-analysis.md`（ハイフン区切り、英語）
- フロントマターに `title`, `description`, `pubDate`, `heroImage` を含める
- **heroImage（アイキャッチ画像）は全記事で同一フォーマットを使う**：720×360px・「シーズン M-2 使用率ランキング表」形式（左パネルにシーズンバッジ＋ポケモン名＋タイプ＋採用率情報、右パネルに該当ポケモン±数体の使用率ランキング表）。タイプベースのカラーパレットで背景・ハイライト色を決定する。新規記事のheroImage生成は `scripts/generate_hero_draft.py` を流用するか、既存スクリプト（`scripts/generate_hero_images.py`）と同じ仕様で生成する。**ポケモン1体大画像＋テキストオーバーレイのような別フォーマットは作らない**（一覧ページで他記事と並んだ際の統一感を損なう）
- 記事はポケモン対戦の分析視点で書く（使用率・型・メタへの影響など）
- **公開（`draft: false` に変更）する際は、`pubDate` を必ず公開当日の日付に更新する**（執筆日のまま古い日付で公開しない）
- **考察記事を公開したら、対応するポケモン情報ページ（`src/content/pokemon/{slug}.md`）にも必ずリンクを追加する**。frontmatterに `analysisSlug: '{blog-slug}'` を追記し、本文末尾に「## 考察記事」セクションとリンクカードを追加する。フォーマットは `src/content/pokemon/aegislash.md` を参照。`barbaracle`のようにポケモン情報ページが存在しない場合は作成を検討する
- **`draft: true` の記事へのリンクはポケモン情報ページに追加しない**。記事を公開（`draft: false`）してからリンクを追加する。同じポケモンに複数記事がある場合は公開済みのもののみ掲載し、最新が上になるよう並べる
- **カードのラベルテキスト（「M-2」「M-3」等）は必ずそのカードの`href`が指すシーズンと一致させる**。過去に他シーズンのカードをコピーして新カードを作った際、hrefだけ書き換えてラベル文言の書き換えを忘れる事故が発生した（例: `href="/blog/xxx-analysis-m3/"` なのにラベルが「M-2」のまま）。新規カード追加時・既存カードのコピー時は必ず両方を確認する。
- **公開後のセルフチェック**：記事公開のたびに、対象ポケモンの全考察記事（`ls src/content/blog/ | grep {英語スラッグの語幹}`）と、対応するポケモン情報ページの「## 考察記事」内の`href`一覧を突き合わせ、①公開済みなのにカードが無い記事がないか、②カードのhrefに対応するファイルが実在するか、③ラベルのシーズン表記がhrefと一致しているか、の3点を確認する
- **新規記事は必ずユーザーの最終確認を得てから `draft: false` にし、本番デプロイする**。独立レビューが通った後も、AIが自動でデプロイしてはならない。ユーザーが「デプロイして」「公開して」と明示的に指示した時点で初めてデプロイを実行する。
- **記事の本番公開は必ず git main への commit + push で行う（直接デプロイ禁止）**。Cloudflare Pages は GitHub の main からビルドするため、**main が唯一の正本**。`wrangler pages deploy` 等で main を経由せず直接デプロイすると、次に誰かが main へ push した瞬間の再ビルドで「main に無い記事」が一覧から消える（実際に発生）。記事ファイル(`src/content/blog/*.md`)・hero画像(`src/assets/`)・`src/data/article-meta.json` は公開時に必ず一緒に commit して push する。公開済み記事と main が乖離していないか（`git status` に未push/未commitの記事が残っていないか）を公開のたびに確認する。
- **公開前に必ず独立レビューを実施する**：`draft: false` にする前に `/記事レビュー <ファイルパス>` を実行し、検出された違反を解消してから公開する。レビュー観点は `.claude/commands/記事レビュー.md` に定義（So What原則・セクション一貫性・事実/データ整合・体裁・公平性・タイプ相性表の体裁ほか観点1〜20）
  - **執筆者自身の「自己レビュー」では公開要件を満たさない**。書いた本人は自分の思い込み・盲点（複合タイプ相性の計算ミス、憶測の技名/特性、片面的な相性提示など）に気づけないため、**必ず別エージェント／別工程による独立レビューを通す**こと。実際、自己レビュー済みの記事を独立レビューに通すと、ほぼ全記事で複合タイプ相性の計算誤りや採用率未確認の技名が新たに検出された。
  - **レビュー観点を追加・変更したら、追加前に書かれた既存記事にも遡及してレビューを通す**。レビューは「観点に書いてある項目しか検出できない」ため、新観点は既存記事に対して未適用のまま残る。

## データソース
- ポケモンチャンピオンズ使用率・同居率: https://champs.pokedb.tokyo
- ランキング補助: https://gamewith.jp/pokemon-champions/
- Smogon Usage Stats（参考）

## データ取得ルール
- 使用率・採用率・同居率データはクロール済みDB（`scripts/pokenavi.db`）に保存されているため、ポケモン情報ページ（`src/content/pokemon/`）作成時は**ソースサイトへのアクセス不要**
- チームメイト（同居率TOP10）の順位は**同居率順位**で表示する（使用率順位ではない）
- DBにないデータが必要な場合のみ、ユーザーに確認してからアクセスする
- EVは最大32スケール（旧作の252ではない）。「EV」と表記し、略称（A/B/C/D/S/H）は大文字

## 収益モデル
- 月次分析レポート販売（デジタル商品）
- データ分析ツールのサブスク（将来）
- アフィリエイト（`AffiliateProducts.astro` コンポーネント）

## 開発コマンド
```bash
npm run dev      # 開発サーバー起動
npm run build    # ビルド
npm run preview  # ビルド確認
```
